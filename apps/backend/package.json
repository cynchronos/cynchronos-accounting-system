{
  "name": "@cas/backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "type-check": "tsc --noEmit",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@cas/db": "workspace:*",
    "@cas/validations": "workspace:*",
    "@hono/zod-validator": "^0.5.0",
    "hono": "^4.7.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@cas/config": "workspace:*",
    "@types/bun": "^1.2.0",
    "typescript": "^5.8.0"
  }
}
