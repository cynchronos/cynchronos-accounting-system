/**
 * Database Schema Barrel Export
 *
 * All Drizzle ORM table definitions should be exported from this file.
 * Import and re-export each schema module as it is created.
 *
 * Example:
 *   export * from './users';
 *   export * from './journal-headers';
 */

export {};
